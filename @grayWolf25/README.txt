This is the Gray Wolf's folder, stay out! 

TODO:

--Corny hacker gif command
        >>call Hackerman
        runs similarly to the >>rekt command

--Add a reaction gif command creator
        >>addReaction string gif
        stores string and gif in a text file, when user types ~string, it reacts with that gif